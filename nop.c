#include "monty.h"
/**
 * nop- does nothing
 * @stack: stack that contains all data
 * @code_line_number: the line where the instruction is read
 */
void nop(stack_t **stack, unsigned int code_line_number)
{
	(void)stack;
	(void)code_line_number;
}
